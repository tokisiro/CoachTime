# CoachTime

## 概要
    このアプリケーションは勤怠管理を行うためのWebアプリケーションです。

## 環境構築

### リポジトリの設定

    ①アプリを作成したいディレクトリに移動

    ②リポジトリをクローン
        git clone git@github.com:tokisiro/CoachTime.git

    ③リポジトリ名を変更

    ④リポジトリのURLの変更(開発する場合)

        1 ③で変更したリポジトリと同名のリモートリポジトリをgithubで作成する
        2 ③で変更したリポジトリのあるディレクトリに移動
        3 リポジトリのURLの変更
            git remote set-url origin 作成したリポジトリのurl

### Dockerの設定

    docker-compose up -d --build

### laravelの環境構築

    ①phpコンテナにログインする
        docker-compose exec php bash

    ②composer.jsonに記載されたパッケージのリストをインストールする。
        composer install

### .envファイルの作成

    ①phpコンテナ内で.env.exampleファイルをコピーして.envファイルを作成する
        cp .env.example .env

### Laravelアプリケーションのセキュリティを強化するための暗号化キーを生成

    ①PHPコンテナ内で以下コマンドを実行
        php artisan key:generate

### テーブルの作成

    ①PHPコンテナ内で以下のコマンドを実行
        php artisan migrate

### ダミーデータの作成

    ①PHPコンテナ内で以下のコマンドを実行
        php artisan db:seed


## 使用技術

・laravel Framework:8.83.29
・nginx:1.21.1
・php:8.1.33
・mysql:8.0.26
・phpmyadmin:5.2.2
・mailhog
・fortify
・formrequest


## ER図

    ![alt text](<スクリーンショット 2025-09-19 14.16.40.png>)

## URL

・一般ユーザー

    ・会員登録画面 : http://localhost/register
    ・ログイン画面 : http://localhost/login
    ・勤怠登録画面 : http://localhost/attendance
    ・勤怠一覧画面 : http://localhost/attendance/list
    ・勤怠詳細画面 : http://localhost/attendances/detail/{id}
    ・申請一覧画面 : http://localhost/stamp_correction_request/list


・管理者

    ・管理者ログイン画面 : http://localhost/admin/login
    ・勤怠一覧画面 : http://localhost/admin/attendances
    ・スタッフ一覧画面 : http://localhost/admin/users
    ・スタッフ別勤怠一覧画面 : 
